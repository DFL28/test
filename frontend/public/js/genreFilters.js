// Genre filters functionality
document.addEventListener('DOMContentLoaded', function() {
  const filterForm = document.getElementById('genreFilterForm');

  if (filterForm) {
    // Auto submit on change (already handled by onchange in HTML)
    console.log('Genre filters loaded');
  }
});
